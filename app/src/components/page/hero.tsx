import React from 'react';
import { ENVDATA } from '../../config';

interface HeroProps {}

const Hero: React.FC<HeroProps> = () => {
  return (
    <div>
      <h1>Hero</h1>
    </div>
  );
};

export default Hero


